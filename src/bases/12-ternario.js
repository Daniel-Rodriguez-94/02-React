

const activo = true

// let mensaje = ""

/* if (!activo) {
    mensaje = "Activo"
} else {
    mensaje = "Inactivo"
} */

// const mensaje = (activo) ? "Ativo" : "Inactivo"
// const mensaje = (!activo) ? "Ativo" : null

const mensaje = !activo && "Activo" 

console.log(mensaje);


